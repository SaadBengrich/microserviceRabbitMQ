spring:
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5672
    exchange: account.exchange
    queue: account.queue
    routingkey: account.routingkey

  datasource:
    url: jdbc:mysql://localhost:3306/messaging_accounts_db?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    show-sql: true

server:
  port: 8080